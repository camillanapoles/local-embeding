# TUI Termux

```bash
pip install --no-deps .  # não necessário — rodar direto:
EMB_TUI_BASE=http://127.0.0.1:8080/v1 python3 tui.py
```
Menu: executar prompts FSM, chat rápido, testar endpoint, ver modelos indicados, criar prompts.
Prompts master em `prompts/` (o APK usa cópia em `apps/android/app/src/main/assets/prompts/`).
